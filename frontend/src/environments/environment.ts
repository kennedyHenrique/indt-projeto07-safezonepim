//Exporta configuracoes de ambiente para o app.
export const environment = {
    //indica se o build eh de producao
    production:false,

    //URL base do backend usado pelas chamadas HTTP.
    apiUrl: 'http://localhost:9595',
};